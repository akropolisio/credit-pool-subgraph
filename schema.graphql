type User @entity {
  id: ID!
  pBalance: BigInt!,
  locked: BigInt!,
  credit: BigInt!,
  history: [String!]!
}

type Balance @entity{
  id: ID!,
  user: String!,
  lBalance: BigInt!,
  pBalance: BigInt!,
}

type Debt @entity {
  id: ID!
  debt_id: BigInt,
  apr: BigInt!,
  borrower: Bytes!,
  total: BigInt!,
  repayed: BigInt!,
  pledges: [String!]!,
  staked: BigInt!,
  status: Status!,
}

type Pledge @entity {
  id: ID!,
  proposal_id: ID!,
  pledger: Bytes!,
  lAmount: BigInt!,
  pAmount: BigInt!
}

type Pool @entity {
  id: ID!,
  lBalance: BigInt!,
  lDebt: BigInt!,
  pEnterPrice: BigInt!,
  pExitPrice: BigInt!,
  users: [String]!,
  usersLength: BigInt!,
}

enum Status {
  PROPOSED
  APPROVED
  DECLINED
  PARTIALLY_REPAYED
  CLOSED
}
