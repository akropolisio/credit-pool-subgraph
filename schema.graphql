type Staker @entity {
  id: ID!
  stakedPTK: BigInt!
}

type Credit @entity {
  id: ID!
  sender: Bytes!
  currentDebt: BigInt!
  pledgers: [Bytes!]!
}

type Pool @entity {
  id: ID!,
  lToken: BigInt!,
  pToken: BigInt!,
  loans:
}