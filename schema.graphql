type User @entity {
  id: ID!
  pBalance: BigInt!,
  locked: BigInt!,
  credit: BigInt!,
  history: [String!]!
}

type Balance @entity{
  id: ID!,
  user: String!,
  lBalance: BigInt!,
  pBalance: BigInt!,
}

type Debt @entity {
  id: ID!
  borrower: Bytes!,
  total: BigInt!,
  repayed: BigInt!,
  pledges: [String!]!,
  status: Status!,
  debt_id: BigInt
}

type Pledge @entity {
  id: ID!
  pledger: Bytes!,
  lAmount: BigInt!,
  pAmount: BigInt!
}

type Pool @entity {
  id: ID!,
  lBalance: BigInt!,
  lDebt: BigInt!,
  pEnterPrice: BigInt!,
  pExitPrice: BigInt!,
  users: [String]!,
}

enum Status {
  PROPOSED
  APPROVED
  DECLINED
  PARTIALLY_REPAYED
  CLOSED
}
